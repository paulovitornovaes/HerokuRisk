{
  "name": "risk-js",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "npm run build && vite preview"
  },
  "dependencies": {
    "vite": "^4.3.2"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  }
}
