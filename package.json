{
  "name": "risk-js",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "concurrently \"node src/socket-back.js\" \"tsc && vite build && vite preview --host 0.0.0.0 --port $PORT\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.2",
    "typescript": "^5.0.2",
    "vite": "^4.3.2",
    "concurrently": "^8.1.0"
  }
}
